export const isApiInProgress = (state) => state.apiIndication.inProgress;

export const isApiSuccessInProgress = (state) => state.apiIndication.successInProgress;

export const isApiFailureInProgress = (state) => state.apiIndication.failureInProgress;